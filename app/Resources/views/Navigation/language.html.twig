<ul class="uk-navbar-nav">
    <li>
        <a href="#">{{ 'app.navigation.language-switcher.language'|trans }}</a>
        <div class="uk-navbar-dropdown">
            <ul class="uk-nav uk-navbar-dropdown-nav">
                {% for link, language in language_switcher.getLocalizedLinks(document) %}

                    <li class="{{ language.isActive ? 'uk-active' }}"><a href="{{ link|urlLocale }}">{{ language.display }}</a></li>

                {% endfor %}
            </ul>
        </div>
    </li>
</ul>
